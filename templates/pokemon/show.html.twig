{% extends 'base-admin.html.twig' %}

{% block title %}Pokemon{% endblock %}

{% block body %}
    <h1>Pokemon</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ pokemon.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ pokemon.Name }}</td>
            </tr>
            <tr>
                <th>Description</th>
                <td>{{ pokemon.Description }}</td>
            </tr>
            <tr>
                <th>NumeroNationnal</th>
                <td>{{ pokemon.numeroNationnal }}</td>
            </tr>
            <tr>
                <th>NumeroPokedex</th>
                <td>{{ pokemon.numeroPokedex }}</td>
            </tr>
            <tr>
                <th>Picture</th>
                <td>{{ pokemon.picture }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_pokemon_index') }}">back to list</a>

    <a href="{{ path('app_pokemon_edit', {'id': pokemon.id}) }}">edit</a>

    {{ include('pokemon/_delete_form.html.twig') }}
{% endblock %}
